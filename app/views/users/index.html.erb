<h1>Lista de Usuários</h1>

<% @user.each do |user| %>
<ul>
	<li><%= user.name %></li>
	<li><%= user.email %></li>
	<li><%= link_to "Remove", user, method: :delete, data: { confirm: "Are you sure?" } %></li>
</ul>
<% end %>